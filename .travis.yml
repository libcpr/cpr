language: cpp

compiler:
  - gcc
  - clang

sudo: required

before_install:
  - wget http://www.cmake.org/files/v3.2/cmake-3.2.1.tar.gz
  - tar -xvzf cmake-3.2.1.tar.gz
  - cmake .
  - make
  - make install

before_script:
  - cmake --version
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
