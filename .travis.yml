language: cpp

compiler:
  - gcc
  - clang

sudo: required

before_install:
  - sudo apt-get update && sudo apt-get install -y cmake

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
